export const billPayableAmount = bill => Number(bill.payableAmount ?? bill.PayableAmount ?? (Number(bill.amount ?? bill.Amount ?? 0) + Number(bill.penaltyAmount ?? bill.PenaltyAmount ?? 0) + Number(bill.extraChargeAmount ?? bill.ExtraChargeAmount ?? 0) - Number(bill.creditAmount ?? bill.CreditAmount ?? 0)));
export const paymentStatusLabel = value => ({ Created: 'Not submitted', Submitted: 'Awaiting review', Verified: 'Payment confirmed', Synced: 'Payment confirmed', Failed: 'Needs attention', Rejected: 'Not approved', Approved: 'Approved', Cancelled: 'Cancelled' }[value] || value || 'Unknown');
export const entryLabel = entry => ({ Charge: 'Bill issued', Penalty: 'Late fee', Payment: 'Payment received', Adjustment: Number(entry.debit ?? entry.amount) > 0 ? 'Extra charge added' : 'Amount credited' }[entry.kind] || entry.kind);
